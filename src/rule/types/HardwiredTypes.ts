/**
 * Hardwired type names, required by the application. When type definitions are
 * downloaded from ther server component, these types must be present.
 */
export enum HardwiredTypes {
    STRING = 'string',
    INT = 'int',
    FLOAT = 'float',
    BOOL = 'boolean'
}